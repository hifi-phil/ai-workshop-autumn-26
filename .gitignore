# .NET build artifacts
[Bb]in/
[Oo]bj/

# Node
node_modules/
npm-debug.log*

# Extension build output (generated by `npm run build` in each Client/ folder)
**/wwwroot/App_Plugins/

# Umbraco runtime cache, logs, temp uploads (regenerated on startup)
**/umbraco/Data/TEMP/
**/umbraco/Logs/

# SQLite WAL/SHM sidecar files — the .db itself is committed deliberately (seeded content)
*.db-wal
*.db-shm

# OS / editor
.DS_Store
*.user
.vs/
.idea/

# Unrelated local tool cache (a personal kanban plugin hook, not part of the workshop)
**/kanban-state.json
